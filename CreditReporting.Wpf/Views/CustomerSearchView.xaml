<UserControl x:Class="CreditReporting.Wpf.Views.CustomerSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                <TextBlock Text="Name (full or partial)" />
                <TextBox Text="{Binding NameQuery, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="0,0,8,0">
                <TextBlock Text="SSN last 4" />
                <TextBox Text="{Binding SsnLast4Query, UpdateSourceTrigger=PropertyChanged}" MaxLength="4" />
            </StackPanel>
            <Button Grid.Column="2" Content="Search" IsDefault="True" VerticalAlignment="Bottom"
                    Command="{Binding SearchCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBool}}" />
            <Button Grid.Column="3" Content="View Report" VerticalAlignment="Bottom"
                    Command="{Binding ViewReportCommand}" />
        </Grid>

        <TextBlock Grid.Row="1" Text="{Binding StatusMessage}" Style="{StaticResource StatusText}" />

        <DataGrid Grid.Row="2" Margin="0,8,0,0"
                  ItemsSource="{Binding Results}"
                  SelectedItem="{Binding SelectedCustomer}"
                  MouseDoubleClick="Results_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="*" />
                <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="*" />
                <DataGridTextColumn Header="Date of Birth" Binding="{Binding DateOfBirth, StringFormat=d}" Width="Auto" />
                <DataGridTextColumn Header="SSN" Binding="{Binding SsnMasked}" Width="Auto" />
                <DataGridTextColumn Header="City" Binding="{Binding City}" Width="*" />
                <DataGridTextColumn Header="State" Binding="{Binding State}" Width="Auto" />
                <DataGridTextColumn Header="Open Accounts" Binding="{Binding OpenAccountCount}" Width="Auto" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>

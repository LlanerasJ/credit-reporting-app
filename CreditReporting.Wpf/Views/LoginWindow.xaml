<Window x:Class="CreditReporting.Wpf.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Credit Reporting - Sign In"
        Height="320" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,18">
            <TextBlock Text="Credit Reporting System" FontSize="20" FontWeight="Bold" Foreground="#1F3864" />
            <TextBlock Text="Authorized staff access (demo environment)" Foreground="#777" />
        </StackPanel>

        <TextBlock Grid.Row="1" Text="Username" Margin="0,0,0,2" />
        <TextBox Grid.Row="2" x:Name="UsernameBox" Margin="0,0,0,10"
                 Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="3" Text="Password" Margin="0,0,0,2" />
        <PasswordBox Grid.Row="4" x:Name="PasswordBox" Margin="0,0,0,14"
                     KeyDown="PasswordBox_KeyDown" />

        <StackPanel Grid.Row="5">
            <Button Content="Sign In" IsDefault="True" HorizontalAlignment="Stretch"
                    Click="Login_Click"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBool}}" />
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#C0392B" TextWrapping="Wrap" Margin="0,8,0,0" />
            <TextBlock Text="Demo logins: analyst / Demo123!  ·  admin / Admin123!"
                       Foreground="#999" FontSize="11" Margin="0,10,0,0" />
        </StackPanel>
    </Grid>
</Window>
